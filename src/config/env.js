import { config } from 'dotenv';

config({ path: `.env` }); 

export const { PORT,
    NODE_ENV,
    TWILIO_PHONE_NUMBER,
    TWILIO_AUTH_TOKEN,
    TWILIO_ACCOUNT_SID,
    SMTP_USER,
    SMTP_PASS,
    DATABASE_URL,
    JWT_SECRET,
    JWT_EXPIRES_IN,
    RATE_LIMIT_WINDOW_MS,
    RATE_LIMIT_MAX_REQUESTS,
    JWT_REFRESH_SECRET,
    GEBETA_MAPS_API_KEY,
    REGULATIONS_MIN_LICENSE_LENGTH,
    REGULATIONS_REQUIRED_FIELDS,
    REGULATIONS_ALLOWED_REGIONS,
} = process.env; 